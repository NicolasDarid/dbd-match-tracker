{
  "buildCommand": "pnpm run db:migrate:deploy && pnpm run db:seed && pnpm run build:no-lint",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production"
  }
}
